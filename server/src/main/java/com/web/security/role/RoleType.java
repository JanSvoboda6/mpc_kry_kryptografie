package com.web.security.role;

/**
 * Enum defining role types.
 * Note: Roles are not particularly used in a project but could
 * be utilized for more complicated resource access & feature management.
 */
public enum RoleType
{
    ROLE_USER,
    ROLE_ADMIN
}
